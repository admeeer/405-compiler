int a = 1;

int b = 2;

function int sum(int x, int y, int z){

    x = x - a;
    y = y - b;

    z = x + y;
    write z;

    return x + y + z;
}

int c = 3;

function int product(int v, int w){
    v = v + a;
    w = w + b;
    return v * w;
}

int d = 4;

write a;
write b;
write c;
write d;

write sum(a, b, c);

write product(a, b);

d = sum(a, b, c);